@model IEnumerable<SmaxV2Attendance.Models.Smx_Category>

@{
    ViewBag.Title = "Index";
}

<h2>Category</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CT_NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CT_SHORTNAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CT_CREATED)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CT_MODIFIED)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CT_MODIFIEDBY)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CT_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CT_SHORTNAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CT_CREATED)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CT_MODIFIED)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CT_MODIFIEDBY)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CT_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CT_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CT_ID })
        </td>
    </tr>
}

</table>
